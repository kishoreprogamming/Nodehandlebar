<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="msapplication-TileColor" content="#ffffff">
  <meta name="msapplication-TileImage" content="icons/ms-icon-144x144.png">
  <meta name="theme-color" content="#ffffff">
  <title>Apj Certificate</title>
  <link rel="stylesheet" href="./css/style.css">
  <link rel="icon" type="image/png" sizes="192x192" href="assets/icons/android-icon-192x192.png">
  <link rel="icon" type="image/png" sizes="32x32" href="assets/icons/favicon-32x32.png">
  <link rel="icon" type="image/png" sizes="96x96" href="assets/icons/favicon-96x96.png">
  <link rel="icon" type="image/png" sizes="16x16" href="assets/icons/favicon-16x16.png">
  <link rel="manifest" href="manifest.json">
</head>
<body>
<div class="Certificate-sec">
  <div class="content">
    <div class="logo">
      <img src="/images/apj-logo.png">
    </div>
    <h2>Certificate of Completion</h2>
    <p class="paragraph-one">This certificate is awarded to</p>
    <h1 id="headingname">{{cousers.name}}</h1>
    <p class="paragraph-two">has successfully completed the <span id="duration">{{cousers.duration}}</span><strong> Full Stack Development Course</strong><br/> at Dr APJ Dream Tech from <span id="start">{{cousers.startdate}}</span>  to <span  id="end">{{cousers.enddate}}</span>.<br/>
      We commend their dedication and hard work throughout the program.</p>
<div class="signature">
  <img src="/images/founder_signature.png" width="200" height="200">
</div>
      <h3>Venkatesan Chandhirasekar</h3>
      <p class="paragraph-three">Founder & CEO</p>
  </div>
</div>

</div>
  <script src="assets/js/jquery.min.js"></script>
  <script src="assets/bootstrap/js/popper.js"></script>
  <script src="assets/bootstrap/js/bootstrap.min.js"></script>
</body>
</html>
{{!-- <script>
let name=localStorage.getItem("username");
console.log("--name output--",name)
let headingname = document.getElementById("headingname").textContent;
if (headingname.includes("[name]")) {
        let replaced =headingname.replace("[name]",name);
            document.getElementById("headingname").textContent =replaced;
        }
        else{
            document.getElementById("headingname").textContent = ''

        }
let startdate=localStorage.getItem("startdate");
let enddate=localStorage.getItem("enddate");
document.getElementById("start").textContent=startdate;
    document.getElementById("end").textContent=enddate;


     if(startdate){
let startDate = new Date(startdate);
if(enddate){
     let endDate = new Date(enddate);
    let totalMonths = (endDate.getFullYear() - startDate.getFullYear()) * 12 + (endDate.getMonth() - startDate.getMonth());
    let totalDays = Math.floor((endDate - startDate) / (1000 * 60 * 60 * 24));
    if (endDate.getDate() < startDate.getDate()) {
        totalMonths--;
    }
    let output = totalMonths >= 1 ? `${totalMonths}-months` : `${totalDays}-days`;
    document.getElementById("duration").textContent = output;
}
</script> --}}
<script>
  <script>
    let name = localStorage.getItem("username");
    let startdate = localStorage.getItem("startdate");
    let enddate = localStorage.getItem("enddate");

    console.log("--name output--", name);
    console.log("--startdate output--", startdate);
    console.log("--enddate output--", enddate);

    // Name Replacement
    if (name) {
        document.getElementById("headingname").textContent = name;
    } else {
        document.getElementById("headingname").textContent = "[name]";
    }

    // Start and End Date Replacement
    if (startdate) {
        document.getElementById("start").textContent = startdate;
    } else {
        document.getElementById("start").textContent = "[startdate]";
    }

    if (enddate) {
        document.getElementById("end").textContent = enddate;
    } else {
        document.getElementById("end").textContent = "[enddate]";
    }

    // Calculate Duration
    if (startdate && enddate) {
        let startDate = new Date(startdate);
        let endDate = new Date(enddate);
        let totalMonths = (endDate.getFullYear() - startDate.getFullYear()) * 12 + (endDate.getMonth() - startDate.getMonth());
        let totalDays = Math.floor((endDate - startDate) / (1000 * 60 * 60 * 24));
        if (endDate.getDate() < startDate.getDate()) {
            totalMonths--;
        }
        let output = totalMonths >= 1 ? `${totalMonths}-months` : `${totalDays}-days`;
        document.getElementById("duration").textContent = output;
    } else {
        document.getElementById("duration").textContent = "[duration]";
    }
</script>

</script>